<!DOCTYPE html>
<html>
  <head>
    <%- include ../partials/head %>
  </head>
  <body>
    <div class="container">
      <nav>
        <%- include ../partials/nav %>
      </nav>
      <% if (user) {%>
        <h2>Check out these spots!</h2>
        <h4>Look through the places people have added and check if you want to go!</h4>
        <form class="" action="/restaurants/my/unseen" method="post">
          <button type="button" name="button">Add to my list!</button>
        </form>
        <table>
          <tr>
            <th>Name</th>
            <th>Cuisine</th>
            <th>Neighborhood</th>
            <th>Website</th>
            <th>Add</th>
          </tr>
          <% data.forEach(restaurant => { %>
            <%- include _show %>
          <% }); %>
        </table>
      <% } %>
    </div>
  </body>
</html>
